# Example backend/.env.example
# Copy to `backend/.env` and fill in real values before running the backend.

# MongoDB connection string (include DB name if desired)
DATABASE_URL=mongodb://localhost:27017/refresh_learn

# JWT secret used for signing tokens (change to a secure random string in production)
JWT_SECRET=your_jwt_secret_here

# Gemini / Google GenAI API key for AI features
GEMINI_API_KEY=AIzaSyA4R5RbrvWybo2BwMXr8d9j-i1RaQwbDbE

# Port for the backend server (optional; defaults to 3000 if omitted)
PORT=3001
